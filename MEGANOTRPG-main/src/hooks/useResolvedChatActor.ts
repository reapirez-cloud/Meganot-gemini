// Backward-compatible chat name. The implementation is now the shared
// Character Runtime Resolver adapter used by every character surface.
export { useResolvedCharacterRuntime as useResolvedChatActor } from "./useResolvedCharacterRuntime.ts"
